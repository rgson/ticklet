# Ticket Tool

A command-line tool for ticket management (the bug tracker kind).
Keeps track of tickets, including status, affected files and related notes.

## Getting started

1. Place `ticket.py` somewhere in your `$PATH`, e.g. in `~/bin`.
2. Run `ticket.py -h` for usage information.

## Dependencies

- Python 3
